---
/**
 * MarkdownContent.astro（Starlight 组件覆盖）
 * 用语义化 HTML5 标签包裹文章正文：
 *  - <article>：文章主体
 *  - <section>：承载 Markdown 渲染结果（保留 Starlight 的 .sl-markdown-content 样式类）
 * 仅包裹渲染结果，不破坏 H1–H3 的层级结构（H1 由 PageTitle 输出）。
 */
---
<article class="geo-article">
  <section class="sl-markdown-content">
    <slot />
  </section>
</article>
